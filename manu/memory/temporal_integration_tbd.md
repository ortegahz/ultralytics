# 时域积分与 TBD（检测前跟踪）专题 (Temporal Integration & Track-Before-Detect)

> **版本时间**：2026-09-17
> **定位与使命**：记录"沿轨迹累加多帧证据"这条路线从动机、上界实测、到逐一证伪的完整闭环。回答一个被领导直接追问的核心问题：**"人眼盯着看能认出来，为什么算法不行？"**
> **适用场景**：涉及单帧极限之外的召回突破、难例真值解救、多帧输入设计、或需要向非技术方解释"为什么单帧检测有极限"时必读。

---

## 一、问题起点：人眼能看见，模型看不见

| 事实 | 实测 |
|---|---|
| 单帧目标强度 | 匹配滤波 Z ≈ **3.3σ** |
| 同帧噪声峰（640×512 全图最大） | **5.40σ**（≈32.7 万像素纯噪声的最大值期望） |
| 结论 | **目标低于搜索门限约 1σ → 单帧不可分** |
| 人眼 | 盯 2~3 秒即可识别 |

**人眼的优势不是单帧灵敏度，而是沿运动轨迹的时间积分。** 用户实测观察确认：单帧能看见亮点，但分不清是噪点还是飞行器；看 2~3 秒有了轨迹信息就容易判断。

---

## 二、核心事实：目标时间能量完全相干（Oracle 上界，实测）

**工具**：`manu/probe_oracle_tbd.py`（匹配滤波 Z + GT 轨迹 + 亚像素双线性采样 + 随机背景点做噪声参考）

序列：`wg2022_ir_020_split_03`

| N（帧） | signal_mean | noise_std | **SNR** | gain | √N | PD@3σ |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 1 | 3.275 | 0.8416 | **3.89** | 1.00 | 1.00 | 60.6% |
| 3 | 3.285 | 0.4875 | 6.74 | 1.73 | 1.73 | 85.9% |
| 5 | 3.294 | 0.3777 | **8.72** | 2.24 | 2.24 | 94.7% |
| 9 | 3.319 | 0.2818 | 11.78 | 3.03 | 3.00 | 99.5% |
| 15 | 3.356 | 0.2186 | **15.35** | 3.95 | 3.87 | 100% |
| 21 | 3.422 | 0.1846 | 18.54 | 4.76 | 4.58 | 100% |
| 45 | 3.721 | 0.1254 | 29.67 | 7.63 | 6.71 | 100% |
| 75 | 3.834 | 0.0979 | **39.14** | 10.06 | 8.66 | 100% |

**三条判据同时成立**：
1. `signal_mean` 不衰减（3.275→3.834）→ **目标时间相干**；
2. `noise_std` 严格按 $1/\sqrt{N}$ 收缩（0.8416→0.0979，比值 8.6 ≈ √75=8.66）→ 标准非相干积累；
3. `gain` 紧贴 √N。

**对照序列**（`wg011_03` 5.11→37.89、`DJI_0175_2` 2.85→25.33）行为一致 → 探针可信。

### ⚠️ 重要更正（推翻旧结论）
早期用 **"整数像素对齐 + 取峰值"** 的粗测得到"GT 对齐多帧平均无效（对比度 4.67→4.43，MAD-Z 4.05→3.80）"，据此写过"目标时间不可积 / TBD 期望值低"——**该结论是测量假象，已作废**。
- 原因：2px 目标用整数对齐误差达 25% 尺寸；且"取峰值"对抖动目标天然低估；
- 正确测法：**匹配滤波响应域 + 亚像素双线性采样 + 取均值**。

**因此：单帧无解 ≠ 无解。目标是可检的，瓶颈在"如何在不训练的前提下利用它"。**

---

## 三、工具清单（本轮新增，均在服务器 `manu/`）

| 脚本 | 作用 |
| :--- | :--- |
| `probe_wg020_03_deep.py` | 单序列三通道信号、模型响应、轨迹运动学（**已修坐标 bug**） |
| `analyze_wg020_03_gt_trajectory.py` | GT 轨迹精细审计（**已修坐标 bug**） |
| `measure_all_sequences_scr.py` | 全盘稳健 SCR / MAD-Z 排行榜（**MAD-Z 主指标**） |
| `compare_sequence_response.py` | 跨序列"响应 vs 输入"对照（缓存 + 原生坐标，两空间隔离） |
| `measure_total_energy.py` | 全盘总积分能量代理 `面积×|灰度差|` 排行 |
| `probe_matched_filter.py` | 匹配滤波单帧 ROC（DoG + CFAR，多 σ 配置） |
| `probe_oracle_tbd.py` | **Oracle TBD 上界**（沿 GT 轨迹亚像素积分） |
| `probe_tbd_random_trajectory.py` | 随机平滑轨迹 null 标定（搜索可行性） |
| `probe_tbd_null_calibration.py` | 阈值+串联式 null（**已判定有选择偏差，仅作教训留存**） |

---

## 四、本轮证伪记录（详见 `falsified_archive.md` 第二十~二十二条）

| 路线 | 实测结果 | 定性 |
| :--- | :--- | :--- |
| **匹配滤波独立检测器** | PD@4.0≈57% 需 FP **26.7/帧**；压到 1.56 FP/帧时 PD 仅 37% | **证伪**（无可用工作点） |
| **阈值+串联式 TBD** | 假轨迹平均 Z **3.32** ≈ 目标 3.275；940 帧长假轨迹 norm score 达 147.9 | **证伪**（先阈值的截断偏差，必然不可分） |
| **检测器级融合（模型 + TBD）** | 两套分数**量纲不可比**（0~1 vs 0~10），仲裁规则全是启发式；且 TBD 假阳性**天生时间相干**，任何航迹一致性过滤都拦不住 | **判定不可靠** |

---

## 五、两个必须记住的关键认知

### 1. 悬停目标与静态坏点在"累加"意义下同构
- 坏点：每帧都亮 → "停在 (5,5)"的路径累加 = 真目标累加；
- 悬停目标：停在原地的路径累加 = 同值；
- **推论**：任何以"速度/位移"为判据的抑制手段（如 `vmin`）**必然误伤悬停目标**（记忆里 HC5 正是悬停）。
- **正确判别量是时间上下文（onset / 持久性），而非速度**：坏点全序列一直在；悬停目标中途出现、之后离开。

### 2. 假轨迹的主因是"持久背景结构"，不是随机白噪声
- 随机平滑轨迹 null（`probe_tbd_random_trajectory.py`）实测：
  - `vmax` 从 3.0 收紧到 1.5，`null_std` 与 `null_max` **反而升高**（L=9：0.336→0.437 / 3.328→4.082）；
  - 白噪声模型无法解释（收紧速度应缩小搜索空间、降低极值）；
  - `null_max/σ` 达 7.3~12.2，远超 10⁵ 独立高斯样本的期望 4.8 → **重尾**。
- **解释**：慢轨迹会"锁"在持续亮斑（固定图案噪声 / 静态热结构 / 坏点）上，L 个样本高度相关。
- **对策方向**：持久性抑制（`min_hits`/net-displacement 那套刚性坏点剪枝的思路），**而非速度门槛**。

### 随机轨迹 null 的三轮实测（`wg2022_ir_020_split_05`，GT=0）

| L | vmax=3.0, M=1e5 | vmax=3.0, M=1e6 | vmax=1.5, M=1e6 | 目标水平 |
| :---: | :---: | :---: | :---: | :---: |
| 5 | 3.234 (#≥tgt 0) | 3.986 (#5) | 4.044 (#23) | 3.29 |
| 9 | 2.990 (0) | 3.328 (#1) | 4.082 (#8) | 3.32 |
| 15 | 2.890 (0) | 2.947 (0) | 3.468 (#2) | 3.36 |
| 25 | 2.286 (0) | 3.396 (#1) | 2.881 (0) | ~3.4 |
| 45 | 1.912 (0) | 1.903 (0) | 2.651 (0) | 3.72 |
| 75 | 1.226 (0) | 1.418 (0) | 1.680 (0) | 3.83 |

**读法**：短窗（≤15 帧）背景最强假轨迹**超过**目标 → 不可分；长窗（≥45 帧）目标胜出。
**注意**：M 从 1e5 增到 1e6 后极值仍在上升（未饱和）→ **外推是必要的，早期 M=1e5 的"无一超过"偏乐观**。

---

## 六、当前唯一推荐路线：速度滤波组特征（**尚未验证**）

### 动机
仲裁问题的唯一原则性解法是**不要两个检测器**，而是把"运动对齐的时序累加"做成**输入特征**，让模型在**同一次决策**里使用它。

### 定义
对每个速度假设 $v=(v_x,v_y)$，把过去 W 帧的匹配滤波响应按 $-v\cdot i$ 平移后取均值：

$$A_v(x,y) = \frac{1}{W}\sum_{i=0}^{W-1} Z_{t-i}\left(x - i\,v_x,\; y - i\,v_y\right)$$

一组速度（9~25 个）→ 一组特征通道。

### 为什么它优于"TBD 当检测器"
| 维度 | TBD 检测器 | 速度滤波特征 |
| :--- | :--- | :--- |
| 仲裁问题 | 有（无解） | **无**（单决策者） |
| 测试轨迹数 | DP 穷举所有弯曲路径 → 虚警膨胀大 | 只测固定速度直线 → 膨胀小 |
| 静态特征 vs 动目标 | 无法区分 | **天然可分**（坏点对所有速度都高，动目标只对匹配速度高） |
| 决策 | 手调阈值 | 模型自己学 |

### 为什么它可能优于历史时序实验
| 历史失败 | 失败原因 | 速度滤波为何不同 |
| :--- | :--- | :--- |
| 时序 Tubelet (K=8) | **空间未对齐致增益归零** | 速度滤波**就是显式对齐** |
| 跨窗口时序卷积 NAS（58 组） | 浅层时序卷积触及天花板 | 这里是**显式给出对齐后的证据**，不是让卷积自学对齐 |

### 尚未解决的风险（必须诚实）
1. **负样本霸权**：无论"加输入通道（需 Twin A/B 重训）"还是"零初始化侧支"，都撞上历史 100% 失败的那道坎（形态学侧支门控退化至 −0.0005、跨窗口 NAS 卡死 0.9064）；
2. **长尾稀有**：该类目标在 43,008 训练帧中占比极低，重训后模型仍可能忽略该特征；
3. **悬停场景**：$v=0$ 滤波器对悬停目标有响应，但坏点也响应 → 仍需持久性/onset 线索。

### 建议的先验验证（零训练、纯 CPU、几分钟）
在动模型之前，用 `wg020_03`（有目标）+ `wg020_05`（纯背景）离线算 $A_v$，只看三个数：

| 指标 | 测什么 | 判据 |
| :--- | :--- | :--- |
| **a. 速度选择性** | 目标处 $A_v$（真实速度）vs 其它速度 | 选择性越强越好 |
| **b. 背景膨胀** | 纯背景上 $\max_v A_v$ 的分布 vs 目标水平 | 越低越好 |
| **c. 坏点可区分性** | 坏点处 $A_v$ 是否对所有速度都高 | 能区分才有价值 |

**注意**：这是 **go/no-go 门槛，不是成功保证**——即使特征干净，仍要面对"负样本霸权"那道坎。

---

## 七、结论与待决

### 已确认
1. **单帧范式下不可分**（3.3σ vs 噪声峰 5.4σ）——这是**单帧检测边界**，非传感器物理极限；
2. **时间能量完全相干**（15 帧→15.4σ），信息充足；
3. **免训练地利用它需要新增检测器 → 仲裁不可靠**；
4. **以速度/位移做抑制会误伤悬停目标**（与坏点同构）。

### 三条可选路径（按推荐度）
| 路径 | 内容 | 成本/风险 |
| :--- | :--- | :--- |
| **A. 收口（推荐）** | 把证据链整理为验收材料，不动生产系统 | 零回归风险；该序列仅占全集 2%，全解 F1 增益 <0.8 点 |
| **B. 零训练特征验证** | 跑第六节的 a/b/c 三个数 | 几分钟；仅作 go/no-go |
| **C. 重训（不推荐）** | 速度滤波通道 + Twin A/B | 高成本，且历史"改模型"0 胜 7 负 |

### 汇报口径（可直接用）
> 该目标为 **2×2 像素、仅高出背景 9 个灰度级**，**总积分能量为全部 23 条序列中最低**（比次低低 2.7 倍）。单帧匹配滤波仅得 ~3.3σ，**低于同帧噪声最大起伏 5.40σ**，故**单帧检测范式**下不可分。而沿真实轨迹的亚像素积分证明其时间能量完全相干（**15 帧达 15.4σ，增益严格符合 √N**）——**信息充足、目标可检**，瓶颈在于当前"先检测后跟踪"架构要求单帧高置信度才能起轨，而该目标仅 2% 帧满足。**能力缺口在检测范式，不在传感器极限。**
